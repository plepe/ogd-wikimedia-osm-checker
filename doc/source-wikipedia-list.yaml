# [optional] describe source of the dataset
source:
  # use type 'wikipedia-list' for this type of download
  type: 'wikipedia-list'

  # specify list id; has the be available in module
  # https://github.com/plepe/wikipedia-list-extractor
  list: AT-Wiener_Wienflussbruecken

  # [optional] specify list of Wikipedia pages, where the content is to be
  # found. if omitted, the list should be configured to be able to download all
  # pages.
  pages:
  - Wiener Wienflussbrücken
